export function enhanceMaturityUI(html){const css=`<style>.maturity-panel{margin:0 0 18px;padding:16px}.maturity-head{display:flex;justify-content:space-between;gap:12px;align-items:flex-start;flex-wrap:wrap}.maturity-head h2{margin:3px 0 4px;font-size:18px}.maturity-head p{margin:0;color:var(--muted);font:12px/1.5 system-ui,sans-serif}.maturity-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:9px;margin-top:12px}.maturity-item{border:1px solid var(--line);border-radius:12px;padding:10px}.maturity-item strong{display:block;margin-bottom:3px;font-size:12px}.maturity-item span{color:var(--muted);font:11px/1.4 system-ui,sans-serif}.maturity-grade{border:1px solid var(--brass);border-radius:999px;padding:6px 10px;color:var(--brass);font:800 10px system-ui,sans-serif;text-transform:uppercase;letter-spacing:.06em}@media(max-width:760px){.maturity-grid{grid-template-columns:1fr}}</style>`;html=html.replace('</head>',css+'</head>');const overview='<div class="workspace active" data-workspace-panel="overview">';html=html.replace(overview,overview+'<div class="card maturity-panel" id="intelligenceMaturity"><div class="maturity-head"><div><span class="eyebrow">System confidence</span><h2>Intelligence maturity</h2><p>How much evidence CuratorOS has accumulated before you rely on its recommendations.</p></div><span class="maturity-grade" id="maturityGrade">Assessing</span></div><div class="maturity-grid" id="maturityGrid"></div></div>');const marker="document.querySelector('#refresh').addEventListener('click',load);";html=html.replace(marker,script()+'\n'+marker);return html}
function script(){return String.raw`
async function renderMaturity(){const grid=document.querySelector('#maturityGrid'),grade=document.querySelector('#maturityGrade'),d=window.__searchIntelligenceData;if(!grid||!grade||!d)return;let watch={ok:false,snapshotCount:0},outs={ok:false,records:[]};try{const [wr,or]=await Promise.all([fetch('/api/watchtower'),fetch('/api/outcomes')]);watch=await wr.json();outs=await or.json()}catch{}const cx=d.curatorContext||{},records=outs.ok?(outs.records||[]):[],implemented=records.filter(x=>x.status==='implemented'),mature=implemented.filter(x=>{const ev=evaluateOutcome(x);return ev.current&&['improved','neutral','declined'].includes(ev.state)});let score=0;if(d.ok!==false)score+=20;if(cx.linkMap?.ok)score+=15;if(cx.siteHealth?.ok)score+=15;if((watch.snapshotCount||0)>=7)score+=15;else if((watch.snapshotCount||0)>=2)score+=8;if(mature.length>=8)score+=20;else if(mature.length>=3)score+=12;else if(mature.length>=1)score+=5;if(records.length>=5)score+=10;else if(records.length)score+=5;const label=score>=85?'Established':score>=65?'Maturing':score>=40?'Developing':'Early';grade.textContent=label;grid.innerHTML=[['Search Console',d.ok===false?'Unavailable':'Live','Current ranking and query data'],['Link Map',cx.linkMap?.ok?'Connected':'Unavailable',cx.linkMap?.ok?'Internal-link graph context':'Graph context not currently available'],['Site Health',cx.siteHealth?.ok?'Connected':'Unavailable',cx.siteHealth?.ok?(cx.siteHealth.checkedPageCount||0)+' active pages checked':'Technical context unavailable'],['Watchtower',(watch.snapshotCount||0)+' snapshots',(watch.snapshotCount||0)>=2?'Historical comparisons active':'Building historical baseline'],['Outcomes',records.length+' tracked',mature.length+' mature enough for evaluation'],['Recommendation memory',mature.length>=8?'Stronger evidence':mature.length>=3?'Early evidence':'Learning',mature.length+' comparable mature outcomes']].map(x=>'<div class="maturity-item"><strong>'+escapeHtml(x[0])+' · '+escapeHtml(String(x[1]))+'</strong><span>'+escapeHtml(String(x[2]))+'</span></div>').join('')}
const maturityWorkspaceRender=renderWorkspaceData;renderWorkspaceData=function(d){maturityWorkspaceRender(d);setTimeout(renderMaturity,0)};const maturityOutcomeRender=renderOutcomes;renderOutcomes=function(){maturityOutcomeRender();setTimeout(renderMaturity,0)};setTimeout(renderMaturity,150);
`;}
