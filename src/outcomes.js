const PREFIX='outcome:';
export async function handleOutcomes(request,env){if(!env.SEARCH_INTELLIGENCE_RECORDS)return json({ok:false,error:'SEARCH_INTELLIGENCE_RECORDS KV binding is not configured.'},503);const url=new URL(request.url);if(request.method==='GET'){const list=await env.SEARCH_INTELLIGENCE_RECORDS.list({prefix:PREFIX,limit:100});const records=[];for(const k of list.keys){const v=await env.SEARCH_INTELLIGENCE_RECORDS.get(k.name,'json');if(v)records.push(v)}records.sort((a,b)=>String(b.updatedAt).localeCompare(String(a.updatedAt)));return json({ok:true,records})}if(request.method!=='POST')return json({ok:false,error:'Method not allowed.'},405);let body;try{body=await request.json()}catch{return json({ok:false,error:'Invalid JSON.'},400)}const action=String(body.action||'save');if(action==='delete'){const id=String(body.id||'');if(!id)return json({ok:false,error:'id required'},400);await env.SEARCH_INTELLIGENCE_RECORDS.delete(PREFIX+id);return json({ok:true,deleted:id})}const now=new Date().toISOString();const id=String(body.id||crypto.randomUUID());const existing=await env.SEARCH_INTELLIGENCE_RECORDS.get(PREFIX+id,'json');const record={id,page:String(body.page||existing?.page||''),query:String(body.query||existing?.query||''),recommendation:String(body.recommendation||existing?.recommendation||''),status:String(body.status||existing?.status||'planned'),notes:String(body.notes??existing?.notes??''),priorityScore:Number(body.priorityScore??existing?.priorityScore??0),baseline:body.baseline||existing?.baseline||null,implementedAt:body.implementedAt||existing?.implementedAt||null,createdAt:existing?.createdAt||now,updatedAt:now};if(record.status==='implemented'&&!record.implementedAt)record.implementedAt=now;if(!record.page&&!record.query)return json({ok:false,error:'page or query required'},400);await env.SEARCH_INTELLIGENCE_RECORDS.put(PREFIX+id,JSON.stringify(record));return json({ok:true,record})}
function json(v,s=200){return new Response(JSON.stringify(v),{status:s,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store'}})}